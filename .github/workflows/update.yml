- name: Generate repo.json raw from delta.webfiles.pro
  run: |
    python3 <<EOF
    import requests

    url = "https://delta.webfiles.pro/get_files.php"
    response = requests.get(url)
    response.raise_for_status()

    with open("repo.json", "w") as f:
        f.write(response.text)
    EOF
